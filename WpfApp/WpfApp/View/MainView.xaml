<Window x:Class="WpfApp.View.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp"
        xmlns:v="clr-namespace:WpfApp.View"
        xmlns:vm="clr-namespace:WpfApp.ViewModel"
        mc:Ignorable="d"
        WindowState="Normal"
        Title="MainWindow" >
    <Window.Resources>
         <DataTemplate x:Key="ListViewTemplate">
             <v:ListViewSystem></v:ListViewSystem>
         </DataTemplate>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"></ColumnDefinition>
            <ColumnDefinition Width="Auto"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <HeaderedContentControl Grid.Column="0">
            <HeaderedContentControl.Header>Источник</HeaderedContentControl.Header>

        <v:ListViewSystem></v:ListViewSystem>
           
            
        </HeaderedContentControl>
        <HeaderedContentControl Grid.Column="1">
            <HeaderedContentControl.Header>Группа уведомлений</HeaderedContentControl.Header>

            <v:ListViewSystem>
            </v:ListViewSystem>
        </HeaderedContentControl>

    </Grid>
</Window>
